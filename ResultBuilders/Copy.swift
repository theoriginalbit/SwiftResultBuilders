enum Copy {
    enum Header: String {
        case communicationPreferences = "Communication Preferences"
        case helpAndSupport = "Help & Support"
        case yourDetails = "Your Details"
    }

    enum Footer: String {
        case deleteWarning = "Deleting your account is permanent, and cannot be recovered."
    }
}
